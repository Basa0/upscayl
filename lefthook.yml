pre-commit:
  commands:
    prettier:
      glob:
        - "*.{ts,js,html,css,json,md,yml,yaml,xml,sh}"
        - "*.MD"
      exclude:
        - "**/*.rs"
        - "src-tauri/Cargo.lock"
      run: npx prettier --write {staged_files}
      stage_fixed: true
